# Maven  wrapper 脚本（Linux/macOS 版本）使用 LF 换行符
/mvnw text eol=lf

# Windows 批处理脚本使用 CRLF 换行符
*.cmd text eol=crlf

# 代码文件统一使用 LF 换行符（跨平台协作推荐）
*.java text eol=lf
*.xml text eol=lf

# Shell 脚本必须用 LF（否则在 Linux/macOS 上无法执行）
*.sh text eol=lf

# Windows 批处理文件必须用 CRLF
*.bat text eol=crlf