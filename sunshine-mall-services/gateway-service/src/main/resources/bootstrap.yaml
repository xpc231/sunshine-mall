spring:
  application:
    name: gateway-service

  cloud:
    nacos:
      server-addr: 192.168.100.128
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml
          - dataId: shared-redis.yaml  # Redis 配置（用于Token黑名单等场景）